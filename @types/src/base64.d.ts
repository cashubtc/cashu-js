export const uint8ToBase64 = {
  decode: (chars: string) => Uint8Array,
  encode: (uint8array: Uint8Array) => string,
};
export function unescapeBase64Url(str: string): string;
export function escapeBase64Url(str: string): string;
